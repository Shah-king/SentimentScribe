# ─────────────────────────────────────────────────────────────────────────────
# Streamlit secrets — copy to .streamlit/secrets.toml and fill in values.
# On Streamlit Community Cloud, paste these in App Settings → Secrets.
# NEVER commit secrets.toml to git.
# ─────────────────────────────────────────────────────────────────────────────

SUPABASE_URL = "https://xxxx.supabase.co"
SUPABASE_ANON_KEY = "eyJ..."

# URL of your deployed FastAPI (e.g. Render/Railway).
# Leave as localhost for local development.
API_URL = "http://localhost:8000"
