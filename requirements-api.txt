# SentimentScribe — API-only dependencies (used by Render)
# For full dependencies (training, dashboard), see requirements.txt

# Core ML (inference only)
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9

# Auth & database
supabase>=2.4.0
python-jose[cryptography]>=3.3.0
httpx>=0.27.0

# Utilities
pyyaml>=6.0.1
python-dotenv>=1.0.1
